{
  "name": "fetchUser",
  "version": "1.0.0",
  "description": "Delegate process to the other lambda function to fetch the user information",
  "main": "src/main.ts",
  "repository": "https://github.com/approvers/gadis/tree/main/lambda/fetchUser",
  "license": "MIT",
  "scripts": {
    "build": "webpack",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "lint:eslint": "eslint src/**/*.ts",
    "lint:prettier": "prettier -c src/**/*.ts",
    "fix": "pnpm fix:eslint && pnpm fix:prettier",
    "fix:eslint": "pnpm lint:eslint -- --fix",
    "fix:prettier": "pnpm lint:prettier -- -w"
  },
  "dependencies": {
    "aws-lambda": "^1.0.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.84"
  }
}
