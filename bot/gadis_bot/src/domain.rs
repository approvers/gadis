pub struct Role {
    pub name: String,
    pub color: String
}

pub struct DiscordUserInformation {
    pub full_name: String,
    pub nickname: String,
    pub avatar_image: String,
    pub top_role: Role
}
